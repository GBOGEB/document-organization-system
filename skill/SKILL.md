---
name: document-organization-level1
description: Use in document-organization-system to census, modernize, repair, innovate, or perpetuate material-property and outward-artifact capability; run Level-1 checks; coordinate validated artifact receipts; and apply measured PCA or Bradley-Terry diagnostics without substituting statistical ranking for physics or source provenance.
---

# Document Organization Level 1

1. Read `LEVEL1.md` and `level1/ssot.json`.
2. Run `python level1/runtime.py census` and `python level1/runtime.py mip`.
3. Reuse `data/materials.json`, modular JS runtime, regression tests and file indexes; do not clone the property engine.
4. Run `python level1/runtime.py orchestrate` for blocks, agents, role and federation targets.
5. PCA accepts measured numeric rows only; BT accepts explicit pairwise outcomes only. Neither can replace NIST/physics regression evidence.
6. Run `python level1/runtime.py self-test` before claiming Level-1.0. Synthetic fixtures are not project evidence.
7. Outward artifacts may carry exact-SHA/result hashes but may not become bidder/QPS compliance evidence by self-assertion.
