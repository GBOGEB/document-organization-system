# GBOGEB Level 1 — Document Organization System

Status: `PC1_CONTROL_PLANE`
Target: `LEVEL_1_0`
Role: `OUTWARD_ARTIFACT_PROPERTY_WORKER`

## Index

Human Level-1 navigator. Machine state: `level1/ssot.json`; gate manifest: `level1/manifest.json`; reusable skill: `skill/`; executable Level-1 kernel arrives in PC2.

Existing capability is reused: this repo already exposes a mature browser runtime, `data/materials.json`, modular JavaScript calculation code, extensive regression tests, file indexes, release/handover artifacts and GitHub Pages. Level-1 makes those capabilities interoperable with the common MIP/federation contract; it does not create a competing material-property engine.

MIP cycles: PC1 census/control, PC2 executable skill/runtime/agents, PC3 DMAIC + measured-only PCA/BT + CI orchestration proof. Structural targets are 0.3333 -> 0.7500 -> 1.0000; observed Level-1.0 requires executed green PC3 proof.

## AOD

AOD = **Architecture–Orchestration–Decision**.

### Architecture

Owns outward material-property/dashboard artifacts, deterministic NIST-regression logic, indexes/navigation and release-facing worker outputs.

### Orchestration

Local: `materials SSOT -> JS calculation/runtime -> tests -> outward artifact/index`. Federated: exact-SHA/result/artifact receipts may be routed to KEB/DOW/child through the hub.

### Decision authority

May validate its own numerical/runtime/outward-artifact consistency. Must not transform independent/reference calculations into bidder evidence or QPS compliance, and must preserve source/provenance class in any federated receipt.
